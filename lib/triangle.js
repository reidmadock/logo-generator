const Shape = require('./shape.js');
/* The triangle class will need to override renderTextEl, since it needs to offset text lower than the other classes */
class Trianagle extends Shape {

}

module.exports = Trianagle;